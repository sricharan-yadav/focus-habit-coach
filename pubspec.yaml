name: focus_habit_coach
description: AI-powered study habit coaching app for students
publish_to: 'none'

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  supabase_flutter: ^2.6.0
  http: ^1.1.0
  intl: ^0.19.0
  percent_indicator: ^4.1.1
  fl_chart: ^0.64.0
  uuid: ^4.0.0
  provider: ^6.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
